/-
Minimal stub for Mathlib.Data.Finset.Basic
-/

-- Finset is defined in core mathlib; we provide a minimal type alias if needed.
-- In core Lean, `Finset` is not available, but for our stubs we can leave it empty
-- since the files using it don't actually construct Finsets in a way that needs
-- the full API.
