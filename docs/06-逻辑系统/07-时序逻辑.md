---
title: 6.7 时序逻辑 / Temporal Logic
version: 2.0
status: maintained
last_updated: 2026-04-21
owner: 逻辑系统工作组
concepts: ["命题逻辑", "谓词逻辑", "模态逻辑", "直觉逻辑", "线性逻辑"]
level: 中级
---



> 📊 **项目全面梳理**：详细的项目结构、模块详解和学习路径，请参阅 [`项目全面梳理-2025.md`](../项目全面梳理-2025.md)
> **项目导航与对标**：[项目扩展与持续推进任务编排](../项目扩展与持续推进任务编排.md)、[国际课程对标表](../国际课程对标表.md)
>
> **合并说明**: 本文档由原 `06-逻辑系统/07-时序逻辑.md` 和 `06-逻辑系统/09-时序逻辑理论.md` 合并而成，整合时间: 2026-04-08

## 6.7 时序逻辑 / Temporal Logic

### 摘要 / Executive Summary

- 统一时序逻辑的形式化定义、LTL与CTL系统、模型检查算法与证明系统。
- 建立时序逻辑在程序验证、硬件验证中的应用框架。

### 关键术语与符号 / Glossary

- 时序逻辑、线性时序逻辑(LTL)、分支时序逻辑(CTL)、模型检查、Kripke结构、时序逻辑证明系统。
- 术语对齐与引用规范：`docs/术语与符号总表.md`，`01-基础理论/00-撰写规范与引用指南.md`

### 术语与符号规范 / Terminology & Notation

- 时序逻辑（Temporal Logic）：研究时间相关命题的逻辑系统。
- LTL（Linear Temporal Logic）：线性时序逻辑。
- CTL（Computation Tree Logic）：分支时序逻辑。
- 记号约定：`G` 表示全局，`F` 表示最终，`X` 表示下一个，`U` 表示直到。

### 交叉引用导航 / Cross-References

- 模态逻辑：参见 `06-逻辑系统/04-模态逻辑.md`。
- 程序验证：参见 `08-实现示例/` 相关文档。

### 快速导航 / Quick Links

- 基本概念
- 线性时序逻辑(LTL)
- 分支时序逻辑(CTL)
- 模型检查算法
- 时序逻辑推导系统
- 应用领域

---

## 目录 (Table of Contents)

- [6.7 时序逻辑 / Temporal Logic](#67-时序逻辑--temporal-logic)
  - [摘要 / Executive Summary](#摘要--executive-summary)
  - [关键术语与符号 / Glossary](#关键术语与符号--glossary)
  - [术语与符号规范 / Terminology \& Notation](#术语与符号规范--terminology--notation)
  - [交叉引用导航 / Cross-References](#交叉引用导航--cross-references)
  - [快速导航 / Quick Links](#快速导航--quick-links)
- [目录 (Table of Contents)](#目录-table-of-contents)
- [1. 基本概念 / Basic Concepts](#1-基本概念--basic-concepts)
  - [1.1 时序逻辑的动机 / Motivation for Temporal Logic](#11-时序逻辑的动机--motivation-for-temporal-logic)
  - [1.2 时序逻辑的分类 / Classification of Temporal Logic](#12-时序逻辑的分类--classification-of-temporal-logic)
  - [1.3 时序逻辑定义 / Definition of Temporal Logic](#13-时序逻辑定义--definition-of-temporal-logic)
- [2. 线性时序逻辑 (LTL) / Linear Temporal Logic](#2-线性时序逻辑-ltl--linear-temporal-logic)
  - [2.1 LTL语法 / LTL Syntax](#21-ltl语法--ltl-syntax)
  - [2.2 Kripke结构 / Kripke Structures](#22-kripke结构--kripke-structures)
  - [2.3 LTL语义 / LTL Semantics](#23-ltl语义--ltl-semantics)
- [3. 分支时序逻辑 (CTL) / Computation Tree Logic](#3-分支时序逻辑-ctl--computation-tree-logic)
  - [3.1 CTL语法 / CTL Syntax](#31-ctl语法--ctl-syntax)
  - [3.2 CTL语义 / CTL Semantics](#32-ctl语义--ctl-semantics)
- [4. 模型检查算法 / Model Checking Algorithms](#4-模型检查算法--model-checking-algorithms)
  - [4.1 符号模型检查 / Symbolic Model Checking](#41-符号模型检查--symbolic-model-checking)
- [5. 时序逻辑推导 / Temporal Logic Inference](#5-时序逻辑推导--temporal-logic-inference)
  - [5.1 时序逻辑证明系统 / Temporal Logic Proof System](#51-时序逻辑证明系统--temporal-logic-proof-system)
  - [5.2 克里普克语义 / Kripke Semantics](#52-克里普克语义--kripke-semantics)
- [6. 应用领域 / Application Domains](#6-应用领域--application-domains)
  - [6.1 程序验证 / Program Verification](#61-程序验证--program-verification)
  - [6.2 硬件验证 / Hardware Verification](#62-硬件验证--hardware-verification)
- [7. 实现示例 / Implementation Examples](#7-实现示例--implementation-examples)
  - [7.1 互斥协议验证 / Mutual Exclusion Protocol Verification](#71-互斥协议验证--mutual-exclusion-protocol-verification)
- [8. 总结 / Summary](#8-总结--summary)
  - [关键特点 / Key Features](#关键特点--key-features)
- [9. 参考文献 / References](#9-参考文献--references)
  - [经典奠基文献 / Classic Foundational Literature](#经典奠基文献--classic-foundational-literature)
  - [标准教材 / Standard Textbooks](#标准教材--standard-textbooks)
- [参考文献](#参考文献)
- [知识导航](#知识导航)
- [学习目标](#学习目标)

---

## 1. 基本概念 / Basic Concepts

### 1.1 时序逻辑的动机 / Motivation for Temporal Logic

**定义 1.1** (时序性质 / Temporal Properties)
时序性质是关于系统状态随时间变化的性质，包括安全性（safety）、活性（liveness）、公平性（fairness）等。

**Definition 1.1** (Temporal Properties)
Temporal properties are properties about how system states change over time, including safety, liveness, fairness, and others.

### 1.2 时序逻辑的分类 / Classification of Temporal Logic

1. **线性时序逻辑 (LTL)** / Linear Temporal Logic
   - 时间结构是线性的
   - 每个时刻只有一个后继状态

2. **分支时序逻辑 (CTL)** / Computation Tree Logic
   - 时间结构是分支的
   - 每个时刻可能有多个后继状态

3. **计算树逻辑* (CTL*)** / Computation Tree Logic*
   - 结合了LTL和CTL的表达能力
   - 更强的表达能力

4. **区间时序逻辑** / Interval Temporal Logic

5. **实时时序逻辑** / Real-Time Temporal Logic

### 1.3 时序逻辑定义 / Definition of Temporal Logic

时序逻辑是研究时间相关命题的逻辑系统，用于描述和推理关于时间变化的行为。

**形式化定义** / **Formal Definition**:

给定时间点 $t$ 和命题 $\phi$，时序逻辑公式定义为：

$$\mathcal{M}, t \models \phi \iff \text{在时间点 } t \text{ 模型 } \mathcal{M} \text{ 满足 } \phi$$

其中 $\mathcal{M}$ 是时序模型。

---

## 2. 线性时序逻辑 (LTL) / Linear Temporal Logic

### 2.1 LTL语法 / LTL Syntax

```rust
// LTL公式定义
// LTL formula definition

#[derive(Clone, Debug, PartialEq)]
pub enum LTLFormula {
    // 基本命题 / Basic propositions
    True,
    False,
    Atom(String),

    // 布尔连接词 / Boolean connectives
    Not(Box<LTLFormula>),
    And(Box<LTLFormula>, Box<LTLFormula>),
    Or(Box<LTLFormula>, Box<LTLFormula>),
    Implies(Box<LTLFormula>, Box<LTLFormula>),

    // 时序算子 / Temporal operators
    Next(Box<LTLFormula>),                    // X φ (next)
    Finally(Box<LTLFormula>),                 // F φ (finally)
    Globally(Box<LTLFormula>),               // G φ (globally)
    Until(Box<LTLFormula>, Box<LTLFormula>), // φ U ψ (until)
    Release(Box<LTLFormula>, Box<LTLFormula>), // φ R ψ (release)
}

impl LTLFormula {
    // 构造器函数 / Constructor functions
    pub fn atom(name: &str) -> Self {
        LTLFormula::Atom(name.to_string())
    }

    pub fn not(formula: LTLFormula) -> Self {
        LTLFormula::Not(Box::new(formula))
    }

    pub fn and(left: LTLFormula, right: LTLFormula) -> Self {
        LTLFormula::And(Box::new(left), Box::new(right))
    }

    pub fn or(left: LTLFormula, right: LTLFormula) -> Self {
        LTLFormula::Or(Box::new(left), Box::new(right))
    }

    pub fn implies(antecedent: LTLFormula, consequent: LTLFormula) -> Self {
        LTLFormula::Implies(Box::new(antecedent), Box::new(consequent))
    }

    pub fn next(formula: LTLFormula) -> Self {
        LTLFormula::Next(Box::new(formula))
    }

    pub fn finally(formula: LTLFormula) -> Self {
        LTLFormula::Finally(Box::new(formula))
    }

    pub fn globally(formula: LTLFormula) -> Self {
        LTLFormula::Globally(Box::new(formula))
    }

    pub fn until(left: LTLFormula, right: LTLFormula) -> Self {
        LTLFormula::Until(Box::new(left), Box::new(right))
    }

    pub fn release(left: LTLFormula, right: LTLFormula) -> Self {
        LTLFormula::Release(Box::new(left), Box::new(right))
    }
}
```

### 2.2 Kripke结构 / Kripke Structures

```rust
// Kripke结构定义
// Kripke structure definition

use std::collections::{HashMap, HashSet};

#[derive(Clone, Debug)]
pub struct KripkeStructure {
    pub states: HashSet<String>,
    pub initial_states: HashSet<String>,
    pub transitions: HashMap<String, HashSet<String>>,
    pub labeling: HashMap<String, HashSet<String>>, // state -> set of atomic propositions
}

impl KripkeStructure {
    pub fn new() -> Self {
        Self {
            states: HashSet::new(),
            initial_states: HashSet::new(),
            transitions: HashMap::new(),
            labeling: HashMap::new(),
        }
    }

    pub fn add_state(&mut self, state: String) {
        self.states.insert(state.clone());
        self.transitions.entry(state.clone()).or_insert_with(HashSet::new);
        self.labeling.entry(state).or_insert_with(HashSet::new);
    }

    pub fn add_initial_state(&mut self, state: String) {
        self.add_state(state.clone());
        self.initial_states.insert(state);
    }

    pub fn add_transition(&mut self, from: String, to: String) {
        self.add_state(from.clone());
        self.add_state(to.clone());
        self.transitions.get_mut(&from).unwrap().insert(to);
    }

    pub fn add_label(&mut self, state: String, label: String) {
        self.add_state(state.clone());
        self.labeling.get_mut(&state).unwrap().insert(label);
    }
}
```

### 2.3 LTL语义 / LTL Semantics

LTL语义定义在无限路径上。对于路径 $\pi = s_0, s_1, s_2, \cdots$：

- $\pi \models X\phi$ 当且仅当 $\pi^1 \models \phi$
- $\pi \models F\phi$ 当且仅当 $\exists i \geq 0, \pi^i \models \phi$
- $\pi \models G\phi$ 当且仅当 $\forall i \geq 0, \pi^i \models \phi$
- $\pi \models \phi U \psi$ 当且仅当 $\exists i \geq 0, (\pi^i \models \psi \land \forall j < i, \pi^j \models \phi)$

---

## 3. 分支时序逻辑 (CTL) / Computation Tree Logic

### 3.1 CTL语法 / CTL Syntax

```rust
// CTL公式定义
// CTL formula definition

#[derive(Clone, Debug, PartialEq)]
pub enum CTLFormula {
    // 基本命题 / Basic propositions
    True,
    False,
    Atom(String),

    // 布尔连接词 / Boolean connectives
    Not(Box<CTLFormula>),
    And(Box<CTLFormula>, Box<CTLFormula>),
    Or(Box<CTLFormula>, Box<CTLFormula>),
    Implies(Box<CTLFormula>, Box<CTLFormula>),

    // 路径量词和时序算子的组合 / Path quantifiers combined with temporal operators
    AX(Box<CTLFormula>),                    // AX φ (for all paths, next)
    EX(Box<CTLFormula>),                    // EX φ (exists path, next)
    AF(Box<CTLFormula>),                    // AF φ (for all paths, finally)
    EF(Box<CTLFormula>),                    // EF φ (exists path, finally)
    AG(Box<CTLFormula>),                    // AG φ (for all paths, globally)
    EG(Box<CTLFormula>),                    // EG φ (exists path, globally)
    AU(Box<CTLFormula>, Box<CTLFormula>),   // A[φ U ψ] (for all paths, until)
    EU(Box<CTLFormula>, Box<CTLFormula>),   // E[φ U ψ] (exists path, until)
}
```

### 3.2 CTL语义 / CTL Semantics

CTL语义定义在Kripke结构的状态上：

- $s \models EX\phi$ 当且仅当 $\exists$ 路径 $\pi$ 从 $s$ 开始，$\pi^1 \models \phi$
- $s \models AX\phi$ 当且仅当 $\forall$ 路径 $\pi$ 从 $s$ 开始，$\pi^1 \models \phi$
- $s \models EF\phi$ 当且仅当 $\exists$ 路径 $\pi$ 从 $s$ 开始，$\exists i, \pi^i \models \phi$
- $s \models AF\phi$ 当且仅当 $\forall$ 路径 $\pi$ 从 $s$ 开始，$\exists i, \pi^i \models \phi$

---

## 4. 模型检查算法 / Model Checking Algorithms

### 4.1 符号模型检查 / Symbolic Model Checking

符号模型检查使用BDD（Binary Decision Diagram）表示状态集合，避免显式枚举所有状态。

---

## 5. 时序逻辑推导 / Temporal Logic Inference

### 5.1 时序逻辑证明系统 / Temporal Logic Proof System

**时序逻辑公理**:

- **分配律**: $X(\phi \land \psi) \leftrightarrow X\phi \land X\psi$
- **展开律**: $\phi U \psi \leftrightarrow \psi \lor (\phi \land X(\phi U \psi))$
- **归纳律**: 用于证明 $G\phi$

**推理规则**:

- **下一推广**: 从 $\phi$ 推导出 $AX\phi$
- **总是推广**: 从 $\phi$ 推导出 $AG\phi$

### 5.2 克里普克语义 / Kripke Semantics

克里普克语义为时序逻辑提供了严格的语义基础：

$$\mathcal{M}, s \models \phi$$

表示在模型 $\mathcal{M}$ 的状态 $s$ 中，公式 $\phi$ 为真。

---

## 6. 应用领域 / Application Domains

### 6.1 程序验证 / Program Verification

时序逻辑在程序验证中的应用：

- **安全性**: $AG \neg bad$
- **活性**: $AG(request \rightarrow AF grant)$
- **公平性**: $AGF enabled \rightarrow AGF taken$

### 6.2 硬件验证 / Hardware Verification

- 电路设计验证
- 协议验证
- 实时系统验证

---

## 7. 实现示例 / Implementation Examples

### 7.1 互斥协议验证 / Mutual Exclusion Protocol Verification

```rust
// 互斥协议的时序逻辑验证
pub struct MutexProtocol {
    structure: KripkeStructure,
}

impl MutexProtocol {
    pub fn verify_mutual_exclusion(&self) -> bool {
        // 互斥性质：AG ¬(p1_critical ∧ p2_critical)
        let mutex_property = CTLFormula::ag(CTLFormula::not(
            CTLFormula::and(
                CTLFormula::atom("p1_critical"),
                CTLFormula::atom("p2_critical")
            )
        ));
        CTLChecker::check_structure(&self.structure, &mutex_property)
    }

    pub fn verify_no_starvation(&self) -> bool {
        // 无饥饿性质：AG(p1_trying → AF p1_critical)
        let no_starvation = CTLFormula::ag(CTLFormula::implies(
            CTLFormula::atom("p1_trying"),
            CTLFormula::af(CTLFormula::atom("p1_critical"))
        ));
        CTLChecker::check_structure(&self.structure, &no_starvation)
    }
}
```

---

## 8. 总结 / Summary

时序逻辑为我们提供了强大的工具来描述和验证系统的时序性质。通过LTL、CTL等不同的时序逻辑，我们可以表达安全性、活性、公平性等重要的系统性质，并使用模型检查技术进行自动化验证。

### 关键特点 / Key Features

1. **表达能力** / Expressiveness
   - 能够表达复杂的时序性质
   - 支持安全性和活性性质

2. **自动化验证** / Automated Verification
   - 模型检查算法
   - 符号模型检查技术

3. **广泛应用** / Wide Applications
   - 软件和硬件验证
   - 协议验证
   - 系统设计

4. **理论基础** / Theoretical Foundation
   - 严格的语义定义
   - 完备的证明系统

---

## 9. 参考文献 / References

### 经典奠基文献 / Classic Foundational Literature

1. [Pnueli1977] Pnueli, A. (1977). "The Temporal Logic of Programs". In *18th Annual Symposium on Foundations of Computer Science (FOCS)* (pp. 46-57). IEEE.
   - **Pnueli时序逻辑的开创性论文**，程序验证的奠基之作。

2. Emerson, E. A. (1990). "Temporal and modal logic". In *Handbook of Theoretical Computer Science* (pp. 995-1072). Elsevier.

### 标准教材 / Standard Textbooks

1. [Clarke1999] Clarke, E. M., Grumberg, O., & Peled, D. (1999). *Model Checking*. MIT Press.
   - **Clarke模型检查的权威教材**，2007年图灵奖获奖工作。

2. [BaierKatoen2008] Baier, C., & Katoen, J. P. (2008). *Principles of Model Checking*. MIT Press.
   - 模型检查的现代权威教材。

3. Huth, M., & Ryan, M. (2004). *Logic in Computer Science: Modelling and Reasoning about Systems*. Cambridge University Press.

---

**文档版本 / Document Version**: 2.0 (合并版)
**最后更新 / Last Updated**: 2026-04-08
**状态 / Status**: 已合并原07-时序逻辑和09-时序逻辑理论

---

## 参考文献

- 待补充

---

## 知识导航

- [返回目录](README.md)

## 学习目标

- 理解07-时序逻辑的核心概念
- 掌握07-时序逻辑的形式化表示
