# 📦 数据结构操作速查表

> 常用数据结构的操作方法、时间复杂度及代码片段

---

## 1️⃣ 数组 (Array)

### 基本操作

| 操作 | 时间 | 代码示例 (C++) | 代码示例 (Python) |
|-----|------|----------------|-------------------|
| 访问 | O(1) | `arr[i]` | `arr[i]` |
| 修改 | O(1) | `arr[i] = x` | `arr[i] = x` |
| 末尾插入 | O(1)* | `arr.push_back(x)` | `arr.append(x)` |
| 中间插入 | O(n) | `arr.insert(it, x)` | `arr.insert(i, x)` |
| 末尾删除 | O(1) | `arr.pop_back()` | `arr.pop()` |
| 中间删除 | O(n) | `arr.erase(it)` | `del arr[i]` |
| 查找 | O(n) | `std::find(...)` | `x in arr` |
| 排序 | O(n log n) | `std::sort(...)` | `arr.sort()` |

*均摊

### 常用代码片段

```cpp
// C++ vector 操作
vector<int> v = {1, 2, 3, 4, 5};

// 遍历
for (int x : v) cout << x << " ";

// 二分查找（需有序）
auto it = lower_bound(v.begin(), v.end(), 3);

// 去重（需先排序）
sort(v.begin(), v.end());
v.erase(unique(v.begin(), v.end()), v.end());
```

```python
# Python list 操作
arr = [1, 2, 3, 4, 5]

# 切片
sub = arr[1:4]      # [2, 3, 4]
rev = arr[::-1]     # [5, 4, 3, 2, 1]

# 列表推导
squares = [x**2 for x in arr]

# 常用函数
max_val = max(arr)
idx = arr.index(3)  # 查找位置
```

---

## 2️⃣ 链表 (Linked List)

### 单向链表

```cpp
struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(nullptr) {}
};

// 基本操作
void insert(ListNode* head, int val) {
    ListNode* newNode = new ListNode(val);
    newNode->next = head->next;
    head->next = newNode;
}

void delete(ListNode* head) {
    ListNode* temp = head->next;
    head->next = temp->next;
    delete temp;
}

// 反转链表
ListNode* reverse(ListNode* head) {
    ListNode *prev = nullptr, *curr = head;
    while (curr) {
        ListNode* next = curr->next;
        curr->next = prev;
        prev = curr;
        curr = next;
    }
    return prev;
}
```

### 双向链表

| 操作 | 时间 | 说明 |
|-----|------|------|
| 头插 | O(1) | `push_front` |
| 尾插 | O(1) | `push_back` |
| 头删 | O(1) | `pop_front` |
| 尾删 | O(1) | `pop_back` |
| 随机访问 | O(n) | 需遍历 |

```cpp
// C++ list
deque<int> dq;
dq.push_front(1);  // 头插
dq.push_back(2);   // 尾插
dq.pop_front();    // 头删
dq.pop_back();     // 尾删
```

---

## 3️⃣ 栈 (Stack)

### 操作汇总

| 操作 | 时间 | C++ | Python | Java |
|-----|------|-----|--------|------|
| 入栈 | O(1) | `push(x)` | `append(x)` | `push(x)` |
| 出栈 | O(1) | `pop()` | `pop()` | `pop()` |
| 查看栈顶 | O(1) | `top()` | `[-1]` | `peek()` |
| 判空 | O(1) | `empty()` | `not stack` | `isEmpty()` |
| 大小 | O(1) | `size()` | `len(stack)` | `size()` |

### 应用场景

```cpp
// 括号匹配
bool isValid(string s) {
    stack<char> st;
    for (char c : s) {
        if (c == '(') st.push(')');
        else if (c == '[') st.push(']');
        else if (c == '{') st.push('}');
        else if (st.empty() || st.top() != c) return false;
        else st.pop();
    }
    return st.empty();
}

// 单调栈：下一个更大元素
vector<int> nextGreater(vector<int>& nums) {
    int n = nums.size();
    vector<int> res(n, -1);
    stack<int> st;
    for (int i = 0; i < n; i++) {
        while (!st.empty() && nums[st.top()] < nums[i]) {
            res[st.top()] = nums[i];
            st.pop();
        }
        st.push(i);
    }
    return res;
}
```

---

## 4️⃣ 队列 (Queue)

### 基本队列

| 操作 | 时间 | C++ | Python | 说明 |
|-----|------|-----|--------|------|
| 入队 | O(1) | `push(x)` | `append(x)` | 队尾加入 |
| 出队 | O(1) | `pop()` | `popleft()` | 队头移除 |
| 查看队首 | O(1) | `front()` | `[0]` | 不移除 |
| 查看队尾 | O(1) | `back()` | `[-1]` | 不移除 |

### 优先队列（堆）

```cpp
// 最大堆
priority_queue<int> maxHeap;

// 最小堆
priority_queue<int, vector<int>, greater<int>> minHeap;

// 自定义比较
struct Compare {
    bool operator()(const pair<int, int>& a, const pair<int, int>& b) {
        return a.first > b.first;  // 小顶堆
    }
};
priority_queue<pair<int, int>, vector<pair<int, int>>, Compare> pq;
```

```python
# Python heapq（小顶堆）
import heapq

heap = []
heapq.heappush(heap, 3)
heapq.heappush(heap, 1)
min_val = heapq.heappop(heap)  # 1

# 大顶堆技巧
heapq.heappush(heap, -x)  # 存入负数
x = -heapq.heappop(heap)  # 取出取反

# Top K
heapq.nlargest(k, nums)
heapq.nsmallest(k, nums)
```

---

## 5️⃣ 哈希表 (Hash Table)

### 操作对比

| 操作 | 平均 | 最坏 | C++ | Python |
|-----|------|------|-----|--------|
| 插入 | O(1) | O(n) | `insert({k,v})` | `d[k] = v` |
| 删除 | O(1) | O(n) | `erase(k)` | `del d[k]` |
| 查找 | O(1) | O(n) | `find(k)` | `k in d` |
| 访问 | O(1) | O(n) | `operator[]` | `d[k]` |

### 常用技巧

```cpp
// C++ unordered_map
unordered_map<string, int> mp;
mp.reserve(1000);  // 预分配空间，减少rehash

// 计数
for (auto& s : words) mp[s]++;

// 遍历
for (auto& [k, v] : mp) {
    cout << k << ": " << v << endl;
}

// unordered_set
unordered_set<int> seen;
if (seen.count(x)) return true;
seen.insert(x);
```

```python
# Python dict 技巧
from collections import defaultdict, Counter

# 默认值为0的字典
d = defaultdict(int)
d['key'] += 1  # 无需判断存在性

# 计数器
cnt = Counter(words)
most_common = cnt.most_common(3)

# 集合操作
a, b = set([1,2,3]), set([2,3,4])
union = a | b        # 并集
inter = a & b        # 交集
diff = a - b         # 差集
```

---

## 6️⃣ 树 (Tree)

### 二叉树遍历

```cpp
// 递归版本
void preorder(TreeNode* root) {
    if (!root) return;
    visit(root);
    preorder(root->left);
    preorder(root->right);
}

// 非递归前序遍历
vector<int> preorder(TreeNode* root) {
    vector<int> res;
    stack<TreeNode*> st;
    if (root) st.push(root);

    while (!st.empty()) {
        TreeNode* node = st.top(); st.pop();
        res.push_back(node->val);
        if (node->right) st.push(node->right);
        if (node->left) st.push(node->left);
    }
    return res;
}

// 层序遍历（BFS）
vector<vector<int>> levelOrder(TreeNode* root) {
    vector<vector<int>> res;
    if (!root) return res;

    queue<TreeNode*> q;
    q.push(root);

    while (!q.empty()) {
        int size = q.size();
        vector<int> level;
        for (int i = 0; i < size; i++) {
            TreeNode* node = q.front(); q.pop();
            level.push_back(node->val);
            if (node->left) q.push(node->left);
            if (node->right) q.push(node->right);
        }
        res.push_back(level);
    }
    return res;
}
```

### 二叉搜索树操作

| 操作 | 平均 | 最坏 |
|-----|------|------|
| 查找 | O(log n) | O(n) |
| 插入 | O(log n) | O(n) |
| 删除 | O(log n) | O(n) |

---

## 7️⃣ 并查集 (Union-Find)

```cpp
class UnionFind {
    vector<int> parent, rank_;
public:
    UnionFind(int n) {
        parent.resize(n);
        rank_.resize(n);
        for (int i = 0; i < n; i++) {
            parent[i] = i;
            rank_[i] = 0;
        }
    }

    // 路径压缩
    int find(int x) {
        if (parent[x] != x) {
            parent[x] = find(parent[x]);
        }
        return parent[x];
    }

    // 按秩合并
    void unite(int x, int y) {
        int px = find(x), py = find(y);
        if (px == py) return;
        if (rank_[px] < rank_[py]) swap(px, py);
        parent[py] = px;
        if (rank_[px] == rank_[py]) rank_[px]++;
    }

    bool connected(int x, int y) {
        return find(x) == find(y);
    }
};
```

---

## 8️⃣ 树状数组 (Fenwick Tree / BIT)

```cpp
class BIT {
    vector<int> tree;
    int n;
public:
    BIT(int size) {
        n = size;
        tree.resize(n + 1);
    }

    // 在位置 i 增加 delta
    void add(int i, int delta) {
        for (; i <= n; i += i & -i)
            tree[i] += delta;
    }

    // 查询前缀和 [1, i]
    int query(int i) {
        int sum = 0;
        for (; i > 0; i -= i & -i)
            sum += tree[i];
        return sum;
    }

    // 查询区间和 [l, r]
    int rangeQuery(int l, int r) {
        return query(r) - query(l - 1);
    }
};
```

---

## 9️⃣ 线段树 (Segment Tree)

```cpp
class SegmentTree {
    vector<int> tree, lazy;
    int n;

    void build(vector<int>& arr, int node, int l, int r) {
        if (l == r) {
            tree[node] = arr[l];
            return;
        }
        int mid = (l + r) >> 1;
        build(arr, node*2, l, mid);
        build(arr, node*2+1, mid+1, r);
        tree[node] = tree[node*2] + tree[node*2+1];
    }

    void pushDown(int node, int l, int r) {
        if (lazy[node] != 0) {
            int mid = (l + r) >> 1;
            tree[node*2] += lazy[node] * (mid - l + 1);
            tree[node*2+1] += lazy[node] * (r - mid);
            lazy[node*2] += lazy[node];
            lazy[node*2+1] += lazy[node];
            lazy[node] = 0;
        }
    }

public:
    SegmentTree(vector<int>& arr) {
        n = arr.size();
        tree.resize(n * 4);
        lazy.resize(n * 4);
        build(arr, 1, 0, n-1);
    }

    void update(int node, int l, int r, int L, int R, int val) {
        if (L <= l && r <= R) {
            tree[node] += val * (r - l + 1);
            lazy[node] += val;
            return;
        }
        pushDown(node, l, r);
        int mid = (l + r) >> 1;
        if (L <= mid) update(node*2, l, mid, L, R, val);
        if (R > mid) update(node*2+1, mid+1, r, L, R, val);
        tree[node] = tree[node*2] + tree[node*2+1];
    }

    int query(int node, int l, int r, int L, int R) {
        if (L <= l && r <= R) return tree[node];
        pushDown(node, l, r);
        int mid = (l + r) >> 1, res = 0;
        if (L <= mid) res += query(node*2, l, mid, L, R);
        if (R > mid) res += query(node*2+1, mid+1, r, L, R);
        return res;
    }
};
```

---

## 🔟 Trie 树（前缀树）

```cpp
class Trie {
    struct Node {
        Node* children[26];
        bool isEnd;
        Node() {
            memset(children, 0, sizeof(children));
            isEnd = false;
        }
    };
    Node* root;

public:
    Trie() { root = new Node(); }

    void insert(string word) {
        Node* node = root;
        for (char c : word) {
            int idx = c - 'a';
            if (!node->children[idx])
                node->children[idx] = new Node();
            node = node->children[idx];
        }
        node->isEnd = true;
    }

    bool search(string word) {
        Node* node = root;
        for (char c : word) {
            int idx = c - 'a';
            if (!node->children[idx]) return false;
            node = node->children[idx];
        }
        return node->isEnd;
    }

    bool startsWith(string prefix) {
        Node* node = root;
        for (char c : prefix) {
            int idx = c - 'a';
            if (!node->children[idx]) return false;
            node = node->children[idx];
        }
        return true;
    }
};
```

---

## 📌 数据结构选择指南

| 场景 | 推荐数据结构 | 原因 |
|-----|-------------|------|
| 频繁随机访问 | 数组 | O(1) 访问 |
| 频繁插入删除 | 链表 | O(1) 修改 |
| LRU Cache | 哈希+双向链表 | O(1) 查询+更新 |
| 去重/判重 | 哈希集合 | O(1) 查询 |
| 范围查询 | 线段树/树状数组 | O(log n) |
| 最值查询 | 堆 | O(1) 取最值 |
| 前缀匹配 | Trie | O(m) 匹配 |
| 连通性判断 | 并查集 | 近乎 O(1) |
| 有序数据 | 平衡树 | O(log n) 操作 |

---

> 💡 选择合适的数据结构是算法优化的关键第一步！
