# 数学推导标准模板

> **版本**: 1.0
> **创建日期**: 2025-01-25
> **状态**: ✅ 已完成
> **适用范围**: 所有算法复杂度分析和数学推导

---

## 📋 模板说明

本文档定义了算法数学推导的标准模板，确保所有推导的一致性和完整性。

**This document defines the standard template for algorithm mathematical derivations to ensure consistency and completeness of all derivations.**

---

## 1. 模板结构

### 1.1 基本结构

每个算法的数学推导应包含以下部分：

1. **算法描述** (Algorithm Description)
2. **递归关系/递推关系** (Recurrence Relation)
3. **严格数学推导** (Rigorous Mathematical Derivation)
   - 方法1：XXX法
   - 方法2：YYY法
   - 方法3：ZZZ法（可选）
4. **形式化正确性证明** (Formal Correctness Proof)
5. **空间复杂度分析** (Space Complexity Analysis)
6. **学术引用** (Academic Citations)

---

## 2. 详细模板

### 2.1 算法描述部分

```markdown
      **算法描述 / Algorithm Description:**

      ```text
      AlgorithmName(A, parameters):
         // 算法伪代码
         ...
      ```

      **定理 X.X.X** 算法的时间复杂度为 $O(f(n))$。
      **Theorem X.X.X** The time complexity of the algorithm is $O(f(n))$.
```

### 2.2 严格数学推导部分

```markdown
      **严格数学推导 / Rigorous Mathematical Derivation:**

      设 $T(n)$ 为算法处理规模为 $n$ 的输入的时间复杂度。
      Let $T(n)$ be the time complexity of the algorithm for input size $n$.

      **递归关系 / Recurrence Relation:**
      $$
      T(n) = \begin{cases}
      \Theta(1) & \text{if } n \leq 1 \\
      aT(n/b) + f(n) & \text{if } n > 1
      \end{cases}
      $$

      #### X.X.X.1 方法1：展开法 / Method 1: Substitution Method

      **展开法 / Substitution Method:**

      假设 $T(n) = c \cdot f(n)$（其中 $c$ 为常数）。
      Assume $T(n) = c \cdot f(n)$ (where $c$ is a constant).

      **归纳证明 / Inductive Proof:**

      **基础情况 / Base Case**: $T(1) = c \cdot f(1) = \Theta(1)$ ✓

      **归纳假设 / Inductive Hypothesis**:
      假设对于所有 $k < n$，$T(k) \leq c \cdot f(k)$
      Assume for all $k < n$, $T(k) \leq c \cdot f(k)$

      **归纳步骤 / Inductive Step**:
      $$T(n) = aT(n/b) + f(n)$$
      $$\leq a \cdot c \cdot f(n/b) + f(n)$$
      $$= \ldots$$
      $$\leq c \cdot f(n)$$

      因此 $T(n) = O(f(n))$。类似可证 $T(n) = \Omega(f(n))$。
      Therefore $T(n) = O(f(n))$. Similarly, we can prove $T(n) = \Omega(f(n))$.

      #### X.X.X.2 方法2：主定理法 / Method 2: Master Theorem

      **主定理法 / Master Theorem Method:**

      对于递归关系 $T(n) = aT(n/b) + f(n)$，其中：
      For the recurrence $T(n) = aT(n/b) + f(n)$, where:

      - $a = X$（子问题数量 / number of subproblems）
      - $b = Y$（子问题大小比例 / subproblem size ratio）
      - $f(n) = \Theta(Z)$（每次操作时间 / operation time per step）

      计算 $n^{\log_b a} = n^{\log_Y X} = n^Z$
      Compute $n^{\log_b a} = n^{\log_Y X} = n^Z$

      由于 $f(n) = \Theta(n^Z) = \Theta(n^{\log_b a})$，属于主定理情况X。
      Since $f(n) = \Theta(n^Z) = \Theta(n^{\log_b a})$, this is case X of the master theorem.

      因此：$T(n) = \Theta(n^{\log_b a} \log n) = \Theta(f(n) \log n)$
      Therefore: $T(n) = \Theta(n^{\log_b a} \log n) = \Theta(f(n) \log n)$

      #### X.X.X.3 方法3：递归树法 / Method 3: Recursion Tree Method

      **递归树法 / Recursion Tree Method:**

      递归树有 $\log_b n$ 层。
      The recursion tree has $\log_b n$ levels.

      - **第 $0$ 层**: $1$ 个子问题，大小为 $n$，工作量 $\Theta(f(n))$
      - **第 $1$ 层**: $a$ 个子问题，大小为 $n/b$，工作量 $a \cdot \Theta(f(n/b))$
      - ...
      - **第 $\log_b n$ 层**: $a^{\log_b n}$ 个子问题，大小为 $1$，工作量 $\Theta(a^{\log_b n})$

      **Level 0**: 1 subproblem of size $n$, work $\Theta(f(n))$
      **Level 1**: $a$ subproblems of size $n/b$, work $a \cdot \Theta(f(n/b))$
      ...
      **Level $\log_b n$**: $a^{\log_b n}$ subproblems of size 1, work $\Theta(a^{\log_b n})$

      **总工作量**:
      **Total work**:

      $$T(n) = \sum_{i=0}^{\log_b n} a^i \cdot f(n/b^i)$$
      $$= \Theta(f(n) \log n)$$
```

### 2.3 形式化正确性证明部分

```markdown
**定理 X.X.X+1** (算法正确性定理) 算法能够正确解决给定问题。
**Theorem X.X.X+1** (Algorithm Correctness Theorem) The algorithm correctly solves the given problem.

**形式化正确性证明 / Formal Correctness Proof:**

**前置条件 / Precondition**:
- 输入条件描述
- Input condition description

**后置条件 / Postcondition**:
- 输出条件描述
- Output condition description

**循环不变式 / Loop Invariant**（如果适用）:

在每次循环迭代开始时，以下条件成立：
At the start of each loop iteration, the following conditions hold:

1. **条件1**: 描述
   **Condition 1**: Description

2. **条件2**: 描述
   **条件2**: Description

**证明循环不变式 / Prove Loop Invariant:**

**初始化 / Initialization**:
- 初始状态满足不变式
- Initial state satisfies invariant

**保持 / Maintenance**:
- 每次迭代保持不变式
- Each iteration maintains invariant

**终止 / Termination**:
- 终止时满足后置条件
- Termination satisfies postcondition

**强归纳法证明**（如果适用）:

**基础情况 / Base Case**:
- 基础情况正确性
- Base case correctness

**归纳假设 / Inductive Hypothesis**:
假设对于所有 $k < n$，算法对规模 $k$ 的输入正确。
Assume for all $k < n$, algorithm is correct for input size $k$.

**归纳步骤 / Inductive Step**:
- 证明对规模 $n$ 的输入正确
- Prove correctness for input size $n$
```

### 2.4 空间复杂度分析部分

```markdown
**空间复杂度分析 / Space Complexity Analysis:**

算法使用的空间包括：
Space used by the algorithm includes:

- **递归调用栈**: 深度 $O(\log n)$，空间 $O(\log n)$
- **辅助数组**: 大小 $O(n)$，空间 $O(n)$
- **其他变量**: 常数空间 $O(1)$

**Recursion call stack**: Depth $O(\log n)$, space $O(\log n)$
**Auxiliary array**: Size $O(n)$, space $O(n)$
**Other variables**: Constant space $O(1)$

**总空间复杂度**: $O(n)$
**Total space complexity**: $O(n)$
```

### 2.5 学术引用部分

```markdown
**学术引用 / Academic Citations:**
- [Author Year]: Author, A. (Year). "Title." *Journal*, Volume(Issue), Pages. DOI/URL
- [Author Year]: Author, A. (Year). *Book Title* (Edition ed.). Publisher. ISBN
```

---

## 3. 推导方法选择指南

### 3.1 何时使用展开法

- 递归关系简单
- 需要精确的常数因子
- 主定理不适用

### 3.2 何时使用主定理法

- 递归关系符合主定理形式：$T(n) = aT(n/b) + f(n)$
- $f(n)$ 是多项式或对数函数
- 需要快速得到渐近界

### 3.3 何时使用递归树法

- 需要直观理解算法行为
- 递归关系复杂
- 需要分析每一层的工作量

### 3.4 何时使用迭代法

- 递归关系可以展开为有限项
- 需要看到展开模式
- 递归深度固定

---

## 4. 证明方法选择指南

### 4.1 何时使用循环不变式

- 算法包含循环
- 需要证明循环的正确性
- 循环有明确的终止条件

### 4.2 何时使用强归纳法

- 算法是递归的
- 需要证明递归的正确性
- 子问题规模递减

### 4.3 何时使用最优子结构

- 动态规划算法
- 需要证明最优解包含子问题最优解
- 问题可以分解为子问题

---

## 5. 质量检查清单

### 5.1 推导完整性

- [ ] 至少提供一种推导方法
- [ ] 推导过程完整，没有跳步
- [ ] 所有符号有明确定义
- [ ] 推导结果有明确的复杂度表示

### 5.2 证明完整性

- [ ] 有明确的前置条件和后置条件
- [ ] 证明方法选择恰当
- [ ] 证明步骤完整
- [ ] 有终止性证明（如适用）

### 5.3 格式规范性

- [ ] 使用标准数学符号（参考ISO 80000-2）
- [ ] 中英文对照完整
- [ ] 学术引用格式正确
- [ ] 代码块格式正确

---

## 6. 示例：归并排序推导

参见：`docs/09-算法理论/01-算法基础/03-排序算法理论.md` 第2.4节

---

## 7. 参考文档

- [数学符号规范-ISO80000对齐版](数学符号规范-ISO80000对齐版.md)
- [形式化证明标准模板](形式化证明标准模板.md)
- [项目改进任务清单](项目改进任务清单-2025.md)

---

**文档维护**: 项目改进工作组
**最后更新**: 2025-01-25
**下次审查**: 2025-04-25
