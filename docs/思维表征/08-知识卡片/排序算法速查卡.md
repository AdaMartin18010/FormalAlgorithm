# 排序算法速查卡


> **版本**: 1.0
> **创建日期**: 2026-04-19
> **最后更新**: 2026-04-19

## 快速参考表

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         排序算法速查表                                           │
├──────────────┬───────────┬───────────┬───────────┬─────────┬─────────┬──────────┤
│    算法      │   最好    │   平均    │   最坏    │  空间   │  稳定   │   适用   │
├──────────────┼───────────┼───────────┼───────────┼─────────┼─────────┼──────────┤
│ 冒泡排序     │   O(n)    │  O(n²)    │  O(n²)    │  O(1)   │   ✓    │ 教学/小数据│
│ 选择排序     │  O(n²)    │  O(n²)    │  O(n²)    │  O(1)   │   ✗    │ 交换代价高│
│ 插入排序     │   O(n)    │  O(n²)    │  O(n²)    │  O(1)   │   ✓    │ 小/基本有序│
│ 希尔排序     │ O(n log n)│ O(n^1.3)  │  O(n²)    │  O(1)   │   ✗    │ 中等数据  │
├──────────────┼───────────┼───────────┼───────────┼─────────┼─────────┼──────────┤
│ 归并排序     │O(n log n) │O(n log n) │O(n log n) │  O(n)   │   ✓    │ 链表/稳定  │
│ 快速排序     │O(n log n) │O(n log n) │  O(n²)    │O(log n) │   ✗    │ 通用首选  │
│ 堆排序       │O(n log n) │O(n log n) │O(n log n) │  O(1)   │   ✗    │ 内存受限  │
├──────────────┼───────────┼───────────┼───────────┼─────────┼─────────┼──────────┤
│ 计数排序     │  O(n+k)   │  O(n+k)   │  O(n+k)   │  O(k)   │   ✓    │ 范围小整数│
│ 基数排序     │ O(d(n+k)) │ O(d(n+k)) │ O(d(n+k)) │ O(n+k)  │   ✓    │ 固定位数  │
│ 桶排序       │   O(n)    │   O(n)    │  O(n²)    │  O(n)   │   ✓    │ 均匀分布  │
│ TimSort      │   O(n)    │O(n log n) │O(n log n) │  O(n)   │   ✓    │ 真实数据  │
└──────────────┴───────────┴───────────┴───────────┴─────────┴─────────┴──────────┘

注: n=元素个数, k=数据范围, d=数字位数
```

---

## 核心代码模板

### 快速排序 (Quick Sort)

```
═══════════════════════════════════════════════════════════════════════════════
【模板代码】
═══════════════════════════════════════════════════════════════════════════════

void quickSort(int[] arr, int left, int right) {
    if (left >= right) return;

    // 选基准 (三数取中更优)
    int pivot = arr[left + (right - left) / 2];
    int i = left, j = right;

    while (i <= j) {
        while (arr[i] < pivot) i++;
        while (arr[j] > pivot) j--;
        if (i <= j) {
            swap(arr[i], arr[j]);
            i++; j--;
        }
    }

    quickSort(arr, left, j);
    quickSort(arr, i, right);
}

【关键点】
• 时间: 平均 O(n log n), 最坏 O(n²)
• 空间: O(log n) 栈空间
• 不稳定，但可优化为三向切分处理重复元素

【三向快排优化】(处理大量重复元素)
void quickSort3Way(int[] arr, int left, int right) {
    if (left >= right) return;

    int pivot = arr[left];
    int lt = left, gt = right, i = left + 1;

    while (i <= gt) {
        if (arr[i] < pivot) swap(arr[lt++], arr[i++]);
        else if (arr[i] > pivot) swap(arr[i], arr[gt--]);
        else i++;
    }

    quickSort3Way(arr, left, lt - 1);
    quickSort3Way(arr, gt + 1, right);
}
```

### 归并排序 (Merge Sort)

```
═══════════════════════════════════════════════════════════════════════════════
【模板代码】
═══════════════════════════════════════════════════════════════════════════════

void mergeSort(int[] arr, int left, int right) {
    if (left >= right) return;

    int mid = left + (right - left) / 2;
    mergeSort(arr, left, mid);
    mergeSort(arr, mid + 1, right);
    merge(arr, left, mid, right);
}

void merge(int[] arr, int left, int mid, int right) {
    int[] temp = new int[right - left + 1];
    int i = left, j = mid + 1, k = 0;

    while (i <= mid && j <= right) {
        if (arr[i] <= arr[j]) temp[k++] = arr[i++];
        else temp[k++] = arr[j++];
    }

    while (i <= mid) temp[k++] = arr[i++];
    while (j <= right) temp[k++] = arr[j++];

    System.arraycopy(temp, 0, arr, left, temp.length);
}

【关键点】
• 时间: 稳定 O(n log n)
• 空间: O(n) 需要额外数组
• 稳定排序，链表排序首选
• 可用于求逆序对数量

【逆序对计数】
long mergeCount(int[] arr, int left, int mid, int right) {
    // ... 合并代码 ...
    // 当 arr[i] > arr[j] 时，arr[i..mid] 都与 arr[j] 构成逆序对
    // count += mid - i + 1;
}
```

### 堆排序 (Heap Sort)

```
═══════════════════════════════════════════════════════════════════════════════
【模板代码】
═══════════════════════════════════════════════════════════════════════════════

void heapSort(int[] arr) {
    int n = arr.length;

    // 建堆 (从最后一个非叶子节点开始)
    for (int i = n / 2 - 1; i >= 0; i--)
        heapify(arr, n, i);

    // 排序
    for (int i = n - 1; i > 0; i--) {
        swap(arr[0], arr[i]);  // 最大元素放到末尾
        heapify(arr, i, 0);     // 调整剩余堆
    }
}

void heapify(int[] arr, int n, int i) {
    int largest = i;
    int left = 2 * i + 1;
    int right = 2 * i + 2;

    if (left < n && arr[left] > arr[largest]) largest = left;
    if (right < n && arr[right] > arr[largest]) largest = right;

    if (largest != i) {
        swap(arr[i], arr[largest]);
        heapify(arr, n, largest);
    }
}

【关键点】
• 时间: 稳定 O(n log n)
• 空间: O(1) 原地排序
• 不稳定
• 适合内存受限场景

【优先级队列应用】
PriorityQueue<Integer> minHeap = new PriorityQueue<>();  // 小顶堆
PriorityQueue<Integer> maxHeap = new PriorityQueue<>(Collections.reverseOrder());  // 大顶堆
```

### 计数排序 & 基数排序

```
═══════════════════════════════════════════════════════════════════════════════
【计数排序 - 适用于小范围整数】
═══════════════════════════════════════════════════════════════════════════════

void countingSort(int[] arr, int maxVal) {
    int[] count = new int[maxVal + 1];

    // 计数
    for (int num : arr) count[num]++;

    // 重构数组
    int idx = 0;
    for (int i = 0; i <= maxVal; i++) {
        while (count[i]-- > 0) arr[idx++] = i;
    }
}

【稳定版计数排序】
void countingSortStable(int[] arr, int maxVal) {
    int[] count = new int[maxVal + 1];
    int[] output = new int[arr.length];

    for (int num : arr) count[num]++;
    for (int i = 1; i <= maxVal; i++) count[i] += count[i-1];

    for (int i = arr.length - 1; i >= 0; i--) {
        output[count[arr[i]] - 1] = arr[i];
        count[arr[i]]--;
    }

    System.arraycopy(output, 0, arr, 0, arr.length);
}

═══════════════════════════════════════════════════════════════════════════════
【基数排序 - 适用于固定位数】
═══════════════════════════════════════════════════════════════════════════════

void radixSort(int[] arr) {
    int max = Arrays.stream(arr).max().getAsInt();

    for (int exp = 1; max / exp > 0; exp *= 10) {
        countingSortByDigit(arr, exp);
    }
}

void countingSortByDigit(int[] arr, int exp) {
    int[] output = new int[arr.length];
    int[] count = new int[10];

    for (int num : arr) count[(num / exp) % 10]++;
    for (int i = 1; i < 10; i++) count[i] += count[i-1];

    for (int i = arr.length - 1; i >= 0; i--) {
        int digit = (arr[i] / exp) % 10;
        output[count[digit] - 1] = arr[i];
        count[digit]--;
    }

    System.arraycopy(output, 0, arr, 0, arr.length);
}

【关键点】
• 计数排序: O(n+k) 时间, O(k) 空间
• 基数排序: O(d(n+k)) 时间, d=位数
• 都是稳定排序
• 适用于整数或有固定表示的数据
```

---

## 算法选择决策树

```
                    需要排序数据?
                        │
                        ▼
            ┌───────────────────────┐
            │ 数据规模 n 多大?      │
            └───────────┬───────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
    n < 50          n < 1000        n >= 1000
        │               │               │
        ▼               ▼               ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────────────┐
│   插入排序     │ │   希尔排序     │ │    需要稳定性?        │
│   O(n²)但     │ │   或快排       │ │        │              │
│   常数小      │ └───────────────┘ │    是 ──┴── 否         │
└───────────────┘                   │       │       │         │
                                    │       ▼       ▼         │
                                    │  ┌─────────┐ ┌────────┐ │
                                    │  │归并排序 │ │快速排序│ │
                                    │  │O(n log n)│ │O(n log n)│
                                    │  │稳定     │ │ 平均最快│ │
                                    │  └─────────┘ └────────┘ │
                                    │                         │
                                    │    内存极度受限?         │
                                    │        │                │
                                    │       是               │
                                    │        │                │
                                    │        ▼                │
                                    │   ┌─────────┐           │
                                    │   │ 堆排序  │           │
                                    │   │O(n log n)│          │
                                    │   │O(1)空间 │           │
                                    │   └─────────┘           │
                                    └─────────────────────────┘

特殊情况分支:
═══════════════════════════════════════════════════════════════════════════════

数据有特殊性质?
    │
    ├── 整数范围小 (k < n) → 计数排序 O(n+k)
    │
    ├── 固定位数整数 → 基数排序 O(d(n+k))
    │
    ├── 均匀分布浮点数 → 桶排序 O(n) 平均
    │
    └── 大量重复元素 → 三向快速排序
```

---

## 常见应用场景

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         排序算法应用场景                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景 1: 求Top K元素                                                         │
│  ════════════════════════════════════════════════════════════════════════   │
│  解法: 堆排序思想 / 快速选择 (QuickSelect)                                    │
│                                                                             │
│  int findKthLargest(int[] nums, int k) {
│      PriorityQueue<Integer> heap = new PriorityQueue<>();
│      for (int num : nums) {
│          heap.offer(num);
│          if (heap.size() > k) heap.poll();
│      }
│      return heap.peek();
│  }
│  时间: O(n log k)  空间: O(k)
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景 2: 合并K个有序数组                                                     │
│  ════════════════════════════════════════════════════════════════════════   │
│  解法: 最小堆 (多路归并)                                                      │
│                                                                             │
│  时间: O(N log k), N为总元素数, k为数组个数                                   │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景 3: 区间调度问题                                                        │
│  ════════════════════════════════════════════════════════════════════════   │
│  解法: 按结束时间排序 + 贪心                                                  │
│                                                                             │
│  Arrays.sort(intervals, (a, b) -> a[1] - b[1]);
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景 4: 求逆序对数量                                                        │
│  ════════════════════════════════════════════════════════════════════════   │
│  解法: 归并排序 / 树状数组                                                   │
│                                                                             │
│  归并排序变体: 在合并时统计逆序对
│  if (arr[i] > arr[j]) count += mid - i + 1;
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  场景 5: 字符串排序                                                          │
│  ════════════════════════════════════════════════════════════════════════   │
│  解法: 后缀数组、Trie树、或基于比较的排序                                     │
│                                                                             │
│  对大量短字符串: 基数排序 (按字符从后往前)                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 易错点提醒

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         常见错误与注意点                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ⚠️ 快速排序                                                                 │
│  ════════════════════════════════════════════════════════════════════════   │
│  • 基准选择: 随机化或三数取中避免最坏情况 O(n²)                               │
│  • 递归终止: left >= right 时要返回                                          │
│  • 防止溢出: mid = left + (right - left) / 2                                 │
│  • 处理重复: 三向切分优化                                                    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ⚠️ 归并排序                                                                 │
│  ════════════════════════════════════════════════════════════════════════   │
│  • 空间分配: 临时数组大小为 right - left + 1                                  │
│  • 复制回原数组: 别忘了 System.arraycopy                                     │
│  • 稳定排序: arr[i] <= arr[j] 用 <= 保证稳定                                 │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ⚠️ 堆排序                                                                   │
│  ════════════════════════════════════════════════════════════════════════   │
│  • 建堆顺序: 从 n/2 - 1 递减到 0                                             │
│  • 子节点下标: left = 2*i + 1, right = 2*i + 2                               │
│  • 堆大小变化: 每次 heapify 的 n 在减小                                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ⚠️ 计数/基数排序                                                            │
│  ════════════════════════════════════════════════════════════════════════   │
│  • 范围检查: 确保数据在计数数组范围内                                         │
│  • 负数处理: 可以偏移 (num - min)                                            │
│  • 基数排序: 从低位到高位处理                                                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 代码模板速查

```
╔═════════════════════════════════════════════════════════════════════════════╗
║                     Java 排序一行代码                                        ║
╠═════════════════════════════════════════════════════════════════════════════╣
║                                                                             ║
║  数组排序:                                                                  ║
║  Arrays.sort(arr);                           // 快排，O(n log n)            ║
║  Arrays.sort(arr, Collections.reverseOrder()); // 降序                      ║
║                                                                             ║
║  部分排序:                                                                  ║
║  Arrays.sort(arr, fromIndex, toIndex);                                      ║
║                                                                             ║
║  对象排序:                                                                  ║
║  Arrays.sort(arr, (a, b) -> a.val - b.val);                                 ║
║  Arrays.sort(arr, Comparator.comparingInt(a -> a.val));                     ║
║                                                                             ║
║  列表排序:                                                                  ║
║  Collections.sort(list);                                                    ║
║  list.sort(Comparator.naturalOrder());                                      ║
║                                                                             ║
║  优先队列:                                                                  ║
║  PriorityQueue<Integer> pq = new PriorityQueue<>();          // 小顶堆      ║
║  PriorityQueue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder());║
║                                                                             ║
╚═════════════════════════════════════════════════════════════════════════════╝

╔═════════════════════════════════════════════════════════════════════════════╗
║                     Python 排序一行代码                                      ║
╠═════════════════════════════════════════════════════════════════════════════╣
║                                                                             ║
║  列表排序:                                                                  ║
║  arr.sort()                                  # 原地排序                     ║
║  sorted_arr = sorted(arr)                    # 返回新列表                   ║
║  arr.sort(reverse=True)                      # 降序                         ║
║                                                                             ║
║  自定义排序:                                                                ║
║  arr.sort(key=lambda x: x[1])                # 按第二个元素排序             ║
║  arr.sort(key=lambda x: (-x[0], x[1]))       # 先按x[0]降序，再按x[1]升序    ║
║                                                                             ║
║  堆操作:                                                                    ║
║  import heapq                                                               ║
║  heapq.heappush(heap, item)                  # 入堆                         ║
║  smallest = heapq.heappop(heap)              # 出堆(最小)                   ║
║  heapq.heapify(arr)                          # 列表转堆                     ║
║                                                                             ║
╚═════════════════════════════════════════════════════════════════════════════╝
```

---

*本速查卡涵盖了排序算法的核心知识点和代码模板，建议配合实际编程练习使用。*

---

## 参考文献

- 待补充

---

## 知识导航

- [返回目录](README.md)

