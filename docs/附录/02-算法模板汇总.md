# 算法模板汇总


> **版本**: 1.0
> **创建日期**: 2026-04-19
> **最后更新**: 2026-04-19

本文档汇总了常见的算法模板和代码片段，方便快速查阅和使用。

## 目录

- [算法模板汇总](#算法模板汇总)
  - [目录](#目录)
  - [数据结构](#数据结构)
    - [并查集 (Union-Find)](#并查集-union-find)
    - [树状数组 (Fenwick Tree / Binary Indexed Tree)](#树状数组-fenwick-tree--binary-indexed-tree)
    - [线段树 (Segment Tree)](#线段树-segment-tree)
    - [Trie 树 (前缀树)](#trie-树-前缀树)
  - [排序算法](#排序算法)
    - [快速排序](#快速排序)
  - [归并排序](#归并排序)
    - [归并排序](#归并排序-1)
    - [堆排序](#堆排序)
  - [搜索算法](#搜索算法)
    - [二分查找](#二分查找)
    - [深度优先搜索 (DFS)](#深度优先搜索-dfs)
  - [广度优先搜索 (BFS)](#广度优先搜索-bfs)
    - [广度优先搜索 (BFS)](#广度优先搜索-bfs-1)
  - [图算法](#图算法)
    - [Dijkstra 算法 (单源最短路径)](#dijkstra-算法-单源最短路径)
    - [Floyd-Warshall 算法 (全源最短路径)](#floyd-warshall-算法-全源最短路径)
    - [拓扑排序](#拓扑排序)
    - [最小生成树 (Kruskal)](#最小生成树-kruskal)
  - [动态规划](#动态规划)
    - [0/1 背包问题](#01-背包问题)
  - [最长公共子序列 (LCS)](#最长公共子序列-lcs)
    - [最长公共子序列 (LCS)](#最长公共子序列-lcs-1)
  - [最长递增子序列 (LIS)](#最长递增子序列-lis)
    - [最长递增子序列 (LIS)](#最长递增子序列-lis-1)
  - [字符串算法](#字符串算法)
    - [KMP 字符串匹配](#kmp-字符串匹配)
    - [字符串哈希](#字符串哈希)
  - [数学算法](#数学算法)
    - [快速幂](#快速幂)
    - [最大公约数 (GCD) 和 最小公倍数 (LCM)](#最大公约数-gcd-和-最小公倍数-lcm)
    - [素数筛](#素数筛)
    - [组合数计算](#组合数计算)
  - [其他技巧](#其他技巧)
    - [离散化](#离散化)
    - [前缀和与差分](#前缀和与差分)
  - [位运算技巧](#位运算技巧)
    - [位运算技巧](#位运算技巧-1)
  - [参考](#参考)
  - [参考文献](#参考文献)
  - [知识导航](#知识导航)

---

## 数据结构

### 并查集 (Union-Find)

```python
class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
        self.rank = [0] * n
        self.count = n

    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])  # 路径压缩
        return self.parent[x]

    def union(self, x, y):
        px, py = self.find(x), self.find(y)
        if px == py:
            return False
        # 按秩合并
        if self.rank[px] < self.rank[py]:
            px, py = py, px
        self.parent[py] = px
        if self.rank[px] == self.rank[py]:
            self.rank[px] += 1
        self.count -= 1
        return True

    def connected(self, x, y):
        return self.find(x) == self.find(y)
```

### 树状数组 (Fenwick Tree / Binary Indexed Tree)

```python
class FenwickTree:
    def __init__(self, n):
        self.n = n
        self.tree = [0] * (n + 1)

    def update(self, i, delta):
        """在位置 i 增加 delta"""
        while i <= self.n:
            self.tree[i] += delta
            i += i & (-i)

    def query(self, i):
        """查询前缀和 [1, i]"""
        res = 0
        while i > 0:
            res += self.tree[i]
            i -= i & (-i)
        return res

    def range_query(self, l, r):
        """查询区间和 [l, r]"""
        return self.query(r) - self.query(l - 1)
```

### 线段树 (Segment Tree)

```python
class SegmentTree:
    def __init__(self, arr):
        self.n = len(arr)
        self.tree = [0] * (4 * self.n)
        self.build(arr, 1, 0, self.n - 1)

    def build(self, arr, node, start, end):
        if start == end:
            self.tree[node] = arr[start]
        else:
            mid = (start + end) // 2
            self.build(arr, 2 * node, start, mid)
            self.build(arr, 2 * node + 1, mid + 1, end)
            self.tree[node] = self.tree[2 * node] + self.tree[2 * node + 1]

    def update(self, idx, val, node=1, start=0, end=None):
        if end is None:
            end = self.n - 1
        if start == end:
            self.tree[node] = val
        else:
            mid = (start + end) // 2
            if idx <= mid:
                self.update(idx, val, 2 * node, start, mid)
            else:
                self.update(idx, val, 2 * node + 1, mid + 1, end)
            self.tree[node] = self.tree[2 * node] + self.tree[2 * node + 1]

    def query(self, l, r, node=1, start=0, end=None):
        if end is None:
            end = self.n - 1
        if r < start or l > end:
            return 0
        if l <= start and end <= r:
            return self.tree[node]
        mid = (start + end) // 2
        left = self.query(l, r, 2 * node, start, mid)
        right = self.query(l, r, 2 * node + 1, mid + 1, end)
        return left + right
```

### Trie 树 (前缀树)

```python
class TrieNode:
    def __init__(self):
        self.children = {}
        self.is_end = False
        self.count = 0  # 经过该节点的单词数

class Trie:
    def __init__(self):
        self.root = TrieNode()

    def insert(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                node.children[char] = TrieNode()
            node = node.children[char]
            node.count += 1
        node.is_end = True

    def search(self, word):
        node = self.root
        for char in word:
            if char not in node.children:
                return False
            node = node.children[char]
        return node.is_end

    def starts_with(self, prefix):
        node = self.root
        for char in prefix:
            if char not in node.children:
                return False
            node = node.children[char]
        return True

    def count_prefix(self, prefix):
        """统计以 prefix 为前缀的单词数"""
        node = self.root
        for char in prefix:
            if char not in node.children:
                return 0
            node = node.children[char]
        return node.count
```

---

## 排序算法

### 快速排序

```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

# 原地快速排序
def quick_sort_inplace(arr, low=0, high=None):
    if high is None:
        high = len(arr) - 1
    if low < high:
        pi = partition(arr, low, high)
        quick_sort_inplace(arr, low, pi - 1)
        quick_sort_inplace(arr, pi + 1, high)
    return arr

def partition(arr, low, high):
    pivot = arr[high]
    i = low - 1
    for j in range(low, high):
        if arr[j] <= pivot:
            i += 1
            arr[i], arr[j] = arr[j], arr[i]
    arr[i + 1], arr[high] = arr[high], arr[i + 1]
    return i + 1
```

## 归并排序

### 归并排序

```python
def merge_sort(arr):
    if len(arr) <= 1:
        return arr
    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])
    return merge(left, right)

def merge(left, right):
    result = []
    i = j = 0
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1
    result.extend(left[i:])
    result.extend(right[j:])
    return result
```

### 堆排序

```python
import heapq

def heap_sort(arr):
    heap = arr[:]
    heapq.heapify(heap)
    return [heapq.heappop(heap) for _ in range(len(heap))]
```

---

## 搜索算法

### 二分查找

```python
def binary_search(arr, target):
    """返回 target 的索引，如果不存在返回 -1"""
    left, right = 0, len(arr) - 1
    while left <= right:
        mid = (left + right) // 2
        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1

def lower_bound(arr, target):
    """返回第一个 >= target 的索引"""
    left, right = 0, len(arr)
    while left < right:
        mid = (left + right) // 2
        if arr[mid] < target:
            left = mid + 1
        else:
            right = mid
    return left

def upper_bound(arr, target):
    """返回第一个 > target 的索引"""
    left, right = 0, len(arr)
    while left < right:
        mid = (left + right) // 2
        if arr[mid] <= target:
            left = mid + 1
        else:
            right = mid
    return left
```

### 深度优先搜索 (DFS)

```python
def dfs(graph, start, visited=None):
    if visited is None:
        visited = set()
    visited.add(start)
    print(start)  # 处理当前节点
    for neighbor in graph.get(start, []):
        if neighbor not in visited:
            dfs(graph, neighbor, visited)
    return visited

# 迭代版本
def dfs_iterative(graph, start):
    visited = set()
    stack = [start]
    while stack:
        node = stack.pop()
        if node not in visited:
            visited.add(node)
            print(node)
            stack.extend(graph.get(node, []))
    return visited
```

## 广度优先搜索 (BFS)

### 广度优先搜索 (BFS)

```python
from collections import deque

def bfs(graph, start):
    visited = {start}
    queue = deque([start])
    while queue:
        node = queue.popleft()
        print(node)  # 处理当前节点
        for neighbor in graph.get(node, []):
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append(neighbor)
    return visited

# 求最短路径
def bfs_shortest_path(graph, start, end):
    visited = {start}
    queue = deque([(start, [start])])
    while queue:
        node, path = queue.popleft()
        if node == end:
            return path
        for neighbor in graph.get(node, []):
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append((neighbor, path + [neighbor]))
    return None
```

---

## 图算法

### Dijkstra 算法 (单源最短路径)

```python
import heapq

def dijkstra(graph, start):
    """
    graph: dict[node, list[(neighbor, weight)]]
    返回: dict[node, shortest_distance]
    """
    dist = {node: float('inf') for node in graph}
    dist[start] = 0
    pq = [(0, start)]

    while pq:
        d, node = heapq.heappop(pq)
        if d > dist[node]:
            continue
        for neighbor, weight in graph[node]:
            new_dist = dist[node] + weight
            if new_dist < dist[neighbor]:
                dist[neighbor] = new_dist
                heapq.heappush(pq, (new_dist, neighbor))

    return dist
```

### Floyd-Warshall 算法 (全源最短路径)

```python
def floyd_warshall(n, edges):
    """
    n: 节点数
    edges: list[(u, v, weight)]
    返回: dist[i][j] 表示 i 到 j 的最短距离
    """
    INF = float('inf')
    dist = [[INF] * n for _ in range(n)]

    for i in range(n):
        dist[i][i] = 0

    for u, v, w in edges:
        dist[u][v] = w

    for k in range(n):
        for i in range(n):
            for j in range(n):
                if dist[i][k] + dist[k][j] < dist[i][j]:
                    dist[i][j] = dist[i][k] + dist[k][j]

    return dist
```

### 拓扑排序

```python
from collections import deque

def topological_sort(graph):
    """
    graph: dict[node, list[neighbor]]
    返回: 拓扑排序结果或 None（如果存在环）
    """
    in_degree = {node: 0 for node in graph}
    for node in graph:
        for neighbor in graph[node]:
            in_degree[neighbor] += 1

    queue = deque([node for node in graph if in_degree[node] == 0])
    result = []

    while queue:
        node = queue.popleft()
        result.append(node)
        for neighbor in graph[node]:
            in_degree[neighbor] -= 1
            if in_degree[neighbor] == 0:
                queue.append(neighbor)

    if len(result) != len(graph):
        return None  # 存在环
    return result
```

### 最小生成树 (Kruskal)

```python
def kruskal(n, edges):
    """
    n: 节点数
    edges: list[(weight, u, v)]
    返回: 最小生成树的边列表和总权重
    """
    edges.sort()  # 按权重排序
    uf = UnionFind(n)
    mst = []
    total_weight = 0

    for weight, u, v in edges:
        if uf.union(u, v):
            mst.append((u, v, weight))
            total_weight += weight
            if len(mst) == n - 1:
                break

    return mst, total_weight
```

---

## 动态规划

### 0/1 背包问题

```python
def knapsack_01(weights, values, capacity):
    """
    weights: 物品重量列表
    values: 物品价值列表
    capacity: 背包容量
    返回: 最大价值
    """
    n = len(weights)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]

    for i in range(1, n + 1):
        for w in range(capacity + 1):
            if weights[i-1] <= w:
                dp[i][w] = max(dp[i-1][w],
                              dp[i-1][w-weights[i-1]] + values[i-1])
            else:
                dp[i][w] = dp[i-1][w]

    return dp[n][capacity]

# 空间优化版本
def knapsack_01_optimized(weights, values, capacity):
    n = len(weights)
    dp = [0] * (capacity + 1)

    for i in range(n):
        for w in range(capacity, weights[i] - 1, -1):
            dp[w] = max(dp[w], dp[w - weights[i]] + values[i])

    return dp[capacity]
```

## 最长公共子序列 (LCS)

### 最长公共子序列 (LCS)

```python
def lcs(s1, s2):
    """返回最长公共子序列的长度"""
    m, n = len(s1), len(s2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]

    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if s1[i-1] == s2[j-1]:
                dp[i][j] = dp[i-1][j-1] + 1
            else:
                dp[i][j] = max(dp[i-1][j], dp[i][j-1])

    return dp[m][n]

# 返回 LCS 字符串
def lcs_string(s1, s2):
    m, n = len(s1), len(s2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]

    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if s1[i-1] == s2[j-1]:
                dp[i][j] = dp[i-1][j-1] + 1
            else:
                dp[i][j] = max(dp[i-1][j], dp[i][j-1])

    # 回溯构造 LCS
    result = []
    i, j = m, n
    while i > 0 and j > 0:
        if s1[i-1] == s2[j-1]:
            result.append(s1[i-1])
            i -= 1
            j -= 1
        elif dp[i-1][j] > dp[i][j-1]:
            i -= 1
        else:
            j -= 1

    return ''.join(reversed(result))
```

## 最长递增子序列 (LIS)

### 最长递增子序列 (LIS)

```python
import bisect

def lis(nums):
    """返回最长递增子序列的长度"""
    tails = []
    for num in nums:
        idx = bisect.bisect_left(tails, num)
        if idx == len(tails):
            tails.append(num)
        else:
            tails[idx] = num
    return len(tails)

# 返回 LIS 序列
def lis_sequence(nums):
    tails = []
    prev = {}

    for i, num in enumerate(nums):
        idx = bisect.bisect_left(tails, (num, i))
        if idx == len(tails):
            tails.append((num, i))
        else:
            tails[idx] = (num, i)
        if idx > 0:
            prev[i] = tails[idx-1][1]

    # 回溯构造序列
    result = []
    if tails:
        idx = tails[-1][1]
        while idx in prev:
            result.append(nums[idx])
            idx = prev[idx]
        result.append(nums[idx])

    return list(reversed(result))
```

---

## 字符串算法

### KMP 字符串匹配

```python
def build_lps(pattern):
    """构建最长前缀后缀数组"""
    m = len(pattern)
    lps = [0] * m
    length = 0
    i = 1

    while i < m:
        if pattern[i] == pattern[length]:
            length += 1
            lps[i] = length
            i += 1
        else:
            if length != 0:
                length = lps[length - 1]
            else:
                lps[i] = 0
                i += 1

    return lps

def kmp_search(text, pattern):
    """返回所有匹配的起始位置"""
    n, m = len(text), len(pattern)
    if m == 0:
        return []

    lps = build_lps(pattern)
    result = []
    i = j = 0

    while i < n:
        if text[i] == pattern[j]:
            i += 1
            j += 1

            if j == m:
                result.append(i - j)
                j = lps[j - 1]
        else:
            if j != 0:
                j = lps[j - 1]
            else:
                i += 1

    return result
```

### 字符串哈希

```python
class StringHash:
    def __init__(self, s, base=131, mod=10**18 + 9):
        self.s = s
        self.n = len(s)
        self.base = base
        self.mod = mod
        self.prefix = [0] * (self.n + 1)
        self.power = [1] * (self.n + 1)

        for i in range(self.n):
            self.prefix[i + 1] = (self.prefix[i] * base + ord(s[i])) % mod
            self.power[i + 1] = (self.power[i] * base) % mod

    def get_hash(self, l, r):
        """获取子串 s[l:r+1] 的哈希值"""
        return (self.prefix[r + 1] - self.prefix[l] * self.power[r - l + 1]) % self.mod

    def __eq__(self, other, l1, r1, l2, r2):
        """比较两个子串是否相等"""
        if r1 - l1 != r2 - l2:
            return False
        return self.get_hash(l1, r1) == other.get_hash(l2, r2)
```

---

## 数学算法

### 快速幂

```python
def fast_pow(base, exp, mod=None):
    """计算 base^exp，可选取模"""
    result = 1
    while exp > 0:
        if exp & 1:
            result = result * base
            if mod:
                result %= mod
        base = base * base
        if mod:
            base %= mod
        exp >>= 1
    return result
```

### 最大公约数 (GCD) 和 最小公倍数 (LCM)

```python
import math

def gcd(a, b):
    """最大公约数"""
    while b:
        a, b = b, a % b
    return a

def lcm(a, b):
    """最小公倍数"""
    return a // gcd(a, b) * b

def extended_gcd(a, b):
    """扩展欧几里得算法，返回 (g, x, y) 使得 ax + by = gcd(a, b)"""
    if b == 0:
        return (a, 1, 0)
    g, x1, y1 = extended_gcd(b, a % b)
    x = y1
    y = x1 - (a // b) * y1
    return (g, x, y)

def mod_inverse(a, m):
    """求 a 在模 m 下的逆元，不存在返回 None"""
    g, x, _ = extended_gcd(a % m, m)
    if g != 1:
        return None
    return (x % m + m) % m
```

### 素数筛

```python
def sieve_of_eratosthenes(n):
    """返回小于等于 n 的所有素数"""
    is_prime = [True] * (n + 1)
    is_prime[0] = is_prime[1] = False

    for i in range(2, int(n**0.5) + 1):
        if is_prime[i]:
            for j in range(i * i, n + 1, i):
                is_prime[j] = False

    return [i for i in range(2, n + 1) if is_prime[i]]

def linear_sieve(n):
    """线性筛（欧拉筛）"""
    is_prime = [True] * (n + 1)
    primes = []

    for i in range(2, n + 1):
        if is_prime[i]:
            primes.append(i)
        for p in primes:
            if i * p > n:
                break
            is_prime[i * p] = False
            if i % p == 0:
                break

    return primes
```

### 组合数计算

```python
class Combination:
    def __init__(self, n, mod=10**9 + 7):
        self.n = n
        self.mod = mod
        self.fac = [1] * (n + 1)
        self.inv_fac = [1] * (n + 1)

        for i in range(1, n + 1):
            self.fac[i] = self.fac[i - 1] * i % mod

        self.inv_fac[n] = pow(self.fac[n], mod - 2, mod)
        for i in range(n - 1, -1, -1):
            self.inv_fac[i] = self.inv_fac[i + 1] * (i + 1) % mod

    def C(self, n, m):
        """组合数 C(n, m)"""
        if m < 0 or m > n:
            return 0
        return self.fac[n] * self.inv_fac[m] % self.mod * self.inv_fac[n - m] % self.mod

    def P(self, n, m):
        """排列数 P(n, m)"""
        if m < 0 or m > n:
            return 0
        return self.fac[n] * self.inv_fac[n - m] % self.mod
```

---

## 其他技巧

### 离散化

```python
def discretize(arr):
    """将数组离散化，返回离散化后的数组和映射关系"""
    sorted_unique = sorted(set(arr))
    mapping = {v: i for i, v in enumerate(sorted_unique)}
    return [mapping[x] for x in arr], mapping, sorted_unique
```

### 前缀和与差分

```python
# 一维前缀和
def prefix_sum(arr):
    """返回前缀和数组"""
    n = len(arr)
    prefix = [0] * (n + 1)
    for i in range(n):
        prefix[i + 1] = prefix[i] + arr[i]
    return prefix

def range_sum(prefix, l, r):
    """计算区间和 [l, r]"""
    return prefix[r + 1] - prefix[l]

# 二维前缀和
def prefix_sum_2d(matrix):
    """返回二维前缀和数组"""
    m, n = len(matrix), len(matrix[0])
    prefix = [[0] * (n + 1) for _ in range(m + 1)]
    for i in range(m):
        for j in range(n):
            prefix[i + 1][j + 1] = (prefix[i][j + 1] + prefix[i + 1][j]
                                    - prefix[i][j] + matrix[i][j])
    return prefix

def range_sum_2d(prefix, r1, c1, r2, c2):
    """计算矩形区域和"""
    return (prefix[r2 + 1][c2 + 1] - prefix[r1][c2 + 1]
            - prefix[r2 + 1][c1] + prefix[r1][c1])

# 差分数组
def diff_array(arr):
    """构造差分数组"""
    n = len(arr)
    diff = [0] * (n + 1)
    diff[0] = arr[0]
    for i in range(1, n):
        diff[i] = arr[i] - arr[i - 1]
    return diff

def range_add(diff, l, r, val):
    """区间加法 [l, r] 加 val"""
    diff[l] += val
    diff[r + 1] -= val

def recover_from_diff(diff):
    """从差分数组恢复原数组"""
    n = len(diff) - 1
    arr = [0] * n
    arr[0] = diff[0]
    for i in range(1, n):
        arr[i] = arr[i - 1] + diff[i]
    return arr
```

## 位运算技巧

### 位运算技巧

```python
# 常用位运算
def is_power_of_two(n):
    """判断是否为 2 的幂"""
    return n > 0 and (n & (n - 1)) == 0

def count_bits(n):
    """计算二进制中 1 的个数"""
    count = 0
    while n:
        count += 1
        n &= n - 1
    return count

def get_lowbit(n):
    """获取最低位的 1 对应的值"""
    return n & (-n)

def next_permutation(mask):
    """获取下一个子集"""
    c = mask & -mask
    r = mask + c
    return (((r ^ mask) >> 2) // c) | r

# 枚举子集
def enumerate_subsets(mask):
    """枚举 mask 的所有非空子集"""
    sub = mask
    while sub:
        # 处理子集 sub
        yield sub
        sub = (sub - 1) & mask
```

---

## 参考

- [LeetCode](https://leetcode.com/)
- [OI Wiki](https://oi-wiki.org/)
- [CP-Algorithms](https://cp-algorithms.com/)

---

## 参考文献

- 待补充

---

## 知识导航

- [返回目录](README.md)
